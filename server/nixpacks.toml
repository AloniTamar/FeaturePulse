[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npx prisma generate", "npm run build", "npm prune --omit=dev"]

[start]
cmd = "npx prisma migrate deploy && npm start"
